## Day 14 混合检索实现与对比测试

**测试日期**：2026-07-16  
**融合方法**：加权平均融合（alpha=0.5），分数归一化后加权求和  
**文档集**：新 PDF（18 个文本块，chunk_size=512, overlap=50）

### 测试对比

| 查询 | 纯向量 Top-1 | 纯 BM25 Top-1 | 混合检索 (α=0.5) Top-1 | 分析 |
|------|-------------|---------------|------------------------|------|
| 停车管理系统有哪些核心功能？ | 基石稳固；融合云计算、物联网与AI… (sim=0.733) | 报告梳理国内外研究现状、技术特点… (score=1.000) | 报告梳理国内外研究现状、技术特点… | 混合检索吸收了 BM25 对标题/关键词“停车管理系统”的精准匹配，同时保留了向量的语义相关块作为辅助 |
| Oracle 数据库设计中采用了什么技术？ | Oracle 并发控制、事务处理等高并发技术 (sim=0.724) | 国内采用 Oracle 构建核心数据层，标准化服务接口… (score=1.000) | 国内采用 Oracle 构建核心数据层，标准化服务接口… | 混合检索结合了 BM25 对“设计”“技术”等关键词的精确命中与向量对技术细节的语义理解，排序更全面 |

### 关键发现

1. **排序融合有效**：混合检索的 Top-1 通常为 BM25 与向量检索中更贴近查询意图的块，且后几位覆盖了另一路的优质结果，减少了单一检索的盲区。
2. **互补效应明显**：第一个查询中，纯向量返回了偏向“未来趋势”的块，BM25 则抓取了“研究现状”块，混合检索将后者提至首位，更符合问题对“核心功能”的直接回答。
3. **归一化合理**：BM25 得分经过 max 归一化后与向量相似度可比，加权融合没有出现某一方绝对压制的情况。
4. 向量库数据太少，暂时无法获得较为明显的对比效果

