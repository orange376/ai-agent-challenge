## Day 16 RAGAS 评估体系搭建

**评估方式**：绕过 ragas 库（因环境兼容性问题），手动实现 Faithfulness 和 Answer Relevancy 评估逻辑，复用 `call_llm` 函数。
**测试集**：4 个问答对，基于样本 PDF 内容设计。
**评估分数**：
- Faithfulness: 1.000
- Answer Relevancy: 0.750

**分析**：
- 忠实度满分，说明系统“有据可查”，没有幻觉。
- 相关性被“本报告提到了哪些国内外研究？”拉低。该问题的标准答案若过于概括，容易与模型基于文档的详细回答不匹配，在评估中被判不相关。
**后续优化**：调整测试集参考答案，使其更贴近文档实际内容；接入 Context Precision / Context Recall 指标。


更改了切片策略，进行测试
## Day 17 切片策略对比实验

**实验日期**：2026-07-18  
**评估方式**：手动评估（复用 `call_llm` 函数，混合检索 alpha=0.5）  
**测试集**：4 个问答对（与 Day 16 一致）  
**切片器**：`RecursiveCharacterTextSplitter`，分隔符适配中文标点  
**Embedding 模型**：`BAAI/bge-small-zh-v1.5`  

### 对比结果

| 策略 | chunk_size | overlap | 文本块数 | Faithfulness | Answer Relevancy |
|------|------------|---------|----------|--------------|------------------|
| A（细粒度） | 256 | 30 | 17 | 1.000 | 0.625 |
| B（均衡）   | 512 | 50 | 9  | 0.925 | 1.000 |
| C（粗粒度） | 1024 | 100 | 5  | 0.425 | 1.000 |

### 逐策略分析

- **策略 A（256/30）**：**忠实度满分**，因为小块几乎不会包含无关信息，模型回答严格基于检索块。但**相关性最低**，因为有些问题的答案分散在多个块中，3 个块的检索上限可能漏掉关键信息。
- **策略 B（512/50）**：**综合表现最优**。Faithfulness 0.925 依旧很高，Answer Relevancy 达到满分。9 个块在信息覆盖和上下文连贯之间取得了平衡。
- **策略 C（1024/100）**：**忠实度急剧下降**，虽然相关性没问题，但大块引入了太多噪声，模型开始“参考”无关内容进行推理，出现事实偏差。这证明了块过大的危害。

### 结论

推荐在日常和面试演示中使用 **策略 B（512/50）**，理由：
1. 信息覆盖完整，能支撑大多数问题的相关回答。
2. 忠实度仍维持在 0.9 以上，幻觉风险可控。
3. 文本块数量适中（9 块），检索效率高。

策略 A 适合对幻觉零容忍的合规场景（但需增加检索数量 k），策略 C 应避免。


## Day 18 混合检索效果验证

**实验设计**：固定切片策略（512/50）和测试集，仅改变检索方式。
**评估方法**：手动评估（Faithfulness + Answer Relevancy）

| 检索方式 | Faithfulness | Answer Relevancy |
|----------|-------------|------------------|
| 纯向量检索 | 1.000 | 1.000 |
| 纯 BM25   | 1.000 | 1.000 |
| 混合检索 (α=0.5) | 0.875 | 0.800 |

**分析**：
- 纯向量和纯 BM25 双满分，混合检索反而略低，说明当前测试集（4 个问题，文档高度相关）无法有效区分三种检索方式。
- 这不是混合检索的失败，而是测试集缺乏挑战性——三个举重运动员都能轻松举起 1 公斤哑铃，分不出高下。

**真实收获**：
- 当文档质量高、信息密度大时，检索方式的影响被缩小，文档本身是 RAG 系统最重要的基石。
- 需要构造更具区分度的测试集（如模糊查询、跨段落推理问题），才能验证混合检索的真正优势。
- Day 13 的“糖尿病 vs 糖尿病视网膜病变”案例已经证明，在关键词敏感的查询上，BM25 和混合检索能显著优于纯向量。

**后续优化方向**：
- 扩大测试集规模（10+ 问题），加入需要跨文档推理的复杂查询
- 用更大的、主题更多元的文档集合重新实验
- 在面试中诚实展示这个结果，并解释“为什么在小数据集上混合检索不一定更好”


## Day 19 RAG 系统优化（综合轮）

**学习内容**：
- 串联所有优化点：最优切片策略（512/50）+ 混合检索（α=0.5）+ 查询改写
- 创建最终版 `rag_final.py`，整合完整 RAG 流程
- 用 `manual_eval.py` 对最终版进行完整评估，与 Day 12 初版对比
- 拓展学习前沿概念：Agentic RAG、Graph RAG、Self-RAG / Corrective RAG
**出现问题**：
- 评估时出现除零错误，原因是循环头缺失导致 `faith_scores` 为空
- 修复后成功跑出最终分数
**最终评估分数**：
- Faithfulness: 1.000（不变，一直稳定）
- Answer Relevancy: 1.000（从 0.750 提升至满分）
**前沿笔记要点**：
- Agentic RAG：让 RAG 系统拥有自主决策权，动态规划检索策略
- Graph RAG：用知识图谱补充向量检索，解决多跳推理和全局总结问题
- Self-RAG / Corrective RAG：在生成过程中引入自我检查和修正机制，降低幻觉

## Day 19 补充：V2 测试集全面评估

**测试集**：10 个问答对，覆盖直接匹配、跨段落推理、对比总结、具体细节、反幻觉、部分信息六种类型。
**评估分数**：
- Faithfulness: 0.850（10 题平均）
- Answer Relevancy: 1.000（全部满分）

**对比 Day 12 初版（前 4 题公共部分）**：
- Answer Relevancy 从 0.750 提升至 1.000，问题“国内外研究”被查询改写修复。

**新暴露的问题**：
1. 对比总结类问题（Q7）Faithfulness 得 0.50，检索覆盖不全，需要 Agent 分步检索。
2. 反幻觉类问题（Q9）评估 LLM 误判，需优化评估 Prompt 正确处理“正确答案即无答案”的边界情况。

**结论**：优化有效，但复杂查询仍需 Agent 加持，这正好衔接 Day 20 的 Agent 实战。